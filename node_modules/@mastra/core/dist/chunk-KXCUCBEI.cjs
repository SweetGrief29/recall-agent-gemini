'use strict';

var chunkV5WKCX3G_cjs = require('./chunk-V5WKCX3G.cjs');

// src/base.ts
var MastraBase = class {
  component = chunkV5WKCX3G_cjs.RegisteredLogger.LLM;
  logger;
  name;
  telemetry;
  constructor({ component, name }) {
    this.component = component || chunkV5WKCX3G_cjs.RegisteredLogger.LLM;
    this.name = name;
    this.logger = new chunkV5WKCX3G_cjs.ConsoleLogger({ name: `${this.component} - ${this.name}` });
  }
  /**
   * Set the logger for the agent
   * @param logger
   */
  __setLogger(logger) {
    this.logger = logger;
    if (this.component !== chunkV5WKCX3G_cjs.RegisteredLogger.LLM) {
      this.logger.debug(`Logger updated [component=${this.component}] [name=${this.name}]`);
    }
  }
  /**
   * Set the telemetry for the
   * @param telemetry
   */
  __setTelemetry(telemetry) {
    this.telemetry = telemetry;
    if (this.component !== chunkV5WKCX3G_cjs.RegisteredLogger.LLM) {
      this.logger.debug(`Telemetry updated [component=${this.component}] [name=${this.telemetry.name}]`);
    }
  }
  /**
   * Get the telemetry on the vector
   * @returns telemetry
   */
  __getTelemetry() {
    return this.telemetry;
  }
  /* 
    get experimental_telemetry config
    */
  get experimental_telemetry() {
    return this.telemetry ? {
      // tracer: this.telemetry.tracer,
      tracer: this.telemetry.getBaggageTracer(),
      isEnabled: !!this.telemetry.tracer
    } : void 0;
  }
};

exports.MastraBase = MastraBase;
//# sourceMappingURL=chunk-KXCUCBEI.cjs.map
//# sourceMappingURL=chunk-KXCUCBEI.cjs.map